server:
  port: 8082

spring:
  application:
    name: COURSE-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/courseservice
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    enabled: false