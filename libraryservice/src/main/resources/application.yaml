server:
  port: 8084

spring:
  application:
    name: LIBRARY-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/libraryservice
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    enabled: false   # ✅ Eureka disabled